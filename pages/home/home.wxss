.header-on-select {
  width: 100%;
  margin-top: 80px;
  height: 40px;
  line-height: 40px;
  position: relative;
  display: none;
}

.is-on-select .header {
  display: none;
}

.is-on-select .header-on-select {
  display: block;
}

.title {
  font-size: 24px;
  font-weight: 600;
  color: var(--black);
}

.add-button, .menu-button {
  width: 40px;
  height: 40px;
  color: var(--black);
  text-align: center;
  position: absolute;
}

.add-button {
  top: 0;
  right: 50px;
}

.menu-button {
  top: 0;
  right: 0;
}

.iconfont.icon-add, .iconfont.icon-menu {
  font-size: 26px;
}
  
.list {
  max-height: calc(100vh - 160px);
  border-radius: 10px 10px 0 0;
  margin-top: 20px;
  overflow: scroll;
  grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  column-gap: 10px;
  row-gap: 10px;
  grid-auto-rows: max(15vh, 120px);
  display: grid;
  padding-bottom: 20px;
  box-sizing: border-box;
}

.is-on-select .list {
  max-height: calc(100vh - 230px);
}

.device-card {
  background-color: white;
  border-radius: 10px;
  padding: 13px 15px;
  position: relative;
}

.device-name {
  font-size: 15px;
}

.inactive {
  background-color: rgba(255, 255, 255, 0.3);
  color: rgba(0, 0, 0, 0.3);
  border: 1px solid rgba(0, 0, 0, 0.05);
}

.footer-inactive {
  color: rgba(0, 0, 0, 0.3);
}

.footer-inactive .icon-delete, .footer-inactive .icon-rename {
  color: rgba(0, 0, 0, 0.3);
}

.disappear {
  display: none !important;
}

.device-check {
  position: absolute;
  right: 15px;
  bottom: 15px;
  width: 18px;
  height: 18px;
  display: none;
}

.is-on-select .device-check {
  display: inline-block;
}

checkbox .wx-checkbox-input {
	width: 18px;
	height: 18px;
  border-radius: 3px;
  box-sizing: border-box;
  position: absolute;
  top: 0;
  border-width: 2px;
}

checkbox .wx-checkbox-input.wx-checkbox-input-checked {
  background: var(--theme-color);
  border-color:var(--theme-color);
  color: white;
}

checkbox .wx-checkbox-input.wx-checkbox-input-checked::before {
  font-size: 16px;
  font-weight: 600;
}

.select-all, .cancel-select-all, .cancel, .hint {
  font-weight: 600;
  font-size: 17px;
}

.select-all, .cancel, .hint {
  display: inline-block;
}

.cancel-select-all {
  display: none;
}

.select-all, .cancel-select-all, .cancel {
  color: var(--theme-color);
  position: absolute;
}

.hint {
  color: var(--black);
  width: 100%;
  text-align: center;
}

.select-all, .cancel-select-all {
  left: 0;
}

.cancel {
  right: 0;
}

.is-select-all .cancel-select-all {
  display: inline-block;
}

.is-select-all .select-all {
  display: none;
}

.hint-number {
  letter-spacing: 0;
  margin: 0 0.15em 0 0.1em;
}

.footer {
  width: 100%;
  display: none;
  position: fixed;
  background-color: white;
  height: 72px;
  bottom: 0;
  left: 0;
  border-top: 1px solid rgba(0, 0, 0, 0.1);
}

.is-on-select .footer {
  display: block;
}

.rename, .delete {
  height: 72px;
  width: 72px;
  position: absolute;
}

.rename {
  left: 20px;
}

.delete {
  right: 20px;
}

.footer-text {
  width: 100%;
  display: block;
  text-align: center;
  font-size: 12px;
}

.icon-rename, .icon-delete {
  width: 100%;
  text-align: center;
  font-size: 24px;
  margin-top: 13px;
  color: var(--black);
}